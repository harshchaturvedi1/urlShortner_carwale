// Material-UI theme customization options
const themeOptions = {
  palette: {
    // Define primary color scheme
    primary: {
      main: "#1976D2", // Blue shade for primary elements (e.g., buttons, links)
    },
    // Define secondary color scheme
    secondary: {
      main: "#FF9800", // Orange shade for secondary elements (e.g., highlights)
    },
  },
  typography: {
    // Set global font family for the app
    fontFamily: "Roboto, Arial, sans-serif", // Use Roboto with fallbacks for consistent text rendering
  },
};

export default themeOptions;
